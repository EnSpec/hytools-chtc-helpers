# DAGMan settings shared by every per-flight DAG (referenced with CONFIG in the .dag).
# Keep the number of simultaneously idle jobs modest so one flight does not swamp the
# access point's queue; DAGMan submits more as they start.
DAGMAN_MAX_JOBS_IDLE = 50
DAGMAN_MAX_JOBS_SUBMITTED = 200
